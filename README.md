# USACO / Contest Practice (Monorepo)

Structure:
- `src/main/java/problems/dsa/<technique>/<ProblemName>.java`

Each file is self-contained (contest style): helpers live inside the solution file.
See `MOVE_MAP.csv` for the automatic migration mapping (you can edit names/techniques if desired).
